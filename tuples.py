# coding=utf-8
numbers_tuple = (0, 1, 1, 2, 3)
print(numbers_tuple[4])
print('''The major difference between list and tuple is that tuple use round bracket and list use square bracket
second difference is that tuple object cannot be changed after creation but we can manipulate list object
like we want''')